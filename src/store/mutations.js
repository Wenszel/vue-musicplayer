const mutations = {
    
    SET_DIRS(state, dirs) {
        state.dirs = dirs;
    },
    SET_FILES(state, files) {
        state.files = files;
    },
    SET_COVERS(state, covers) {
        state.covers = covers;
    }
}
export default mutations;